package com.hdbank.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class BeanConfig {
    // Domain services are auto-wired via @Component annotations on adapters
    // This config class can be extended for additional bean definitions
}
